{
  "name": "subito-app",
  "description": "A subito micro service with job interface",
  "scripts": {
    "build-dev": "npm run compile-js && NODE_ENV=development node --inspect lib/index.js --now"
  },
  "dependencies": {
    "argv": "0.0.2",
    "node-cron": "3.0.2"
  },
  "devDependencies": {
    "@types/argv": "0.0.5",
    "@types/node": "18.11.7",
    "@types/node-cron": "3.0.5"
  }
}
